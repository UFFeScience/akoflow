# Copy to .env and replace the values before running docker compose.
# Use a high-entropy token: openssl rand -hex 32
AKOFLOW_API_TOKEN=replace-with-a-random-secret
# Comma-separated browser origins. Do not use * for an authenticated API.
AKOFLOW_API_ALLOWED_ORIGINS=http://localhost:3000

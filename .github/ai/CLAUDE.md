# Claude Notes For Akoflow

Read `AGENTS.md` first.

Use `AI_ARCHITECTURE.md` for the overall system map and `AI_RUNTIME_EXECUTION_FLOW.md` for runtime-specific behavior.

## Claude Usage Style

- Prefer the shared architecture file for broad questions.
- Prefer the runtime flow file when tracing execution bugs.
- Keep changes minimal and avoid unrelated edits.

---

id: interface-tour
title: Tour the AkôFlow interface
sidebar_label: Interface tour
description: Find workflows, environments, plans, runs, and results in AkôFlow Desktop.
---

import AnnotatedScreenshot from '@site/src/components/AnnotatedScreenshot';
import useBaseUrl from '@docusaurus/useBaseUrl';

AkôFlow Desktop lets you see your workflows, environments, plans, runs, and results in one place. Start on **Overview** to check the current instance, then use the sidebar to open the record you need.

<AnnotatedScreenshot
  src="/img/interface/overview.png"
  darkSrc="/img/interface/overview-dark.png"
  alt="AkôFlow Desktop overview with the sidebar, global search, notifications, instance status, and workflow summary identified"
  caption="A current Desktop overview. The numbered markers identify the navigation and status controls; counts and workflow names change with the selected instance. Open a workflow row or select View all to continue into a record."
  callouts={[
    {number: 1, x: 4.8, y: 16.0, label: 'The sidebar groups workflows, infrastructure, runs, artifacts, provenance, and audit.'},
    {number: 2, x: 50.0, y: 7.5, label: 'Global search opens entities without first visiting their list page.'},
    {number: 3, x: 92.1, y: 7.5, label: 'Notifications report operations that need attention.'},
    {number: 4, x: 8.2, y: 91.5, label: 'The instance indicator shows which AkôFlow service Desktop is using.'},
  ]}
/>

## Main areas

| Area | What you do there |
|---|---|
| Overview | Check registered environments, workflows, and completed runs |
| Workflows | Create workflows and inspect their activities |
| Planning sessions | Compare candidate plans and select one |
| Infrastructure | Connect environments and inspect available resources |
| Runs | Start and inspect workflow, simulation, and interactive runs |
| Artifacts | Register and build executables for workflows |
| Provenance | [Follow how a result was produced](./data/provenance) |
| Audit | [Inspect the history of operations](./data/audit-events) |

## Follow a record

Breadcrumbs connect a run to its plan and workflow. Select an earlier item to return to that record.

For example:

<img src={useBaseUrl('/img/architecture/interface-ownership-hierarchy.svg')} alt="Workflow pages lead from workflow to plan, execution and activity. Infrastructure pages lead from environment through inventory and resource to provisioning and operation." />

Use the back control below the breadcrumb to return to the parent record, including when you arrived through search or a notification.

## Find a record

Press <kbd>⌘</kbd>+<kbd>K</kbd> on macOS, <kbd>Ctrl</kbd>+<kbd>K</kbd> on Windows or Linux, or select the search field. An empty search shows shortcuts to areas such as **Workflow definitions**, **Planning sessions**, and **Runs**.

Enter a name or ID to find records. Select a result or press <kbd>Enter</kbd> to open the first one; press <kbd>Escape</kbd> to close search.

Open a result's detail page for its status and history. If Desktop says **Search is temporarily unavailable**, check the AkôFlow connection and try again.

## Global terminal

Interactive sessions open in the terminal panel at the bottom of the application. Closing a page does not close a remote session; close it from the panel when you finish.

Open a session from a compatible resource, then use the panel to inspect its output. Export the log from the session detail page when you need a saved record. See [Interactive console](./operations/interactive-console) for prerequisites and recovery.

## Continue

To create and run one workflow in Desktop, follow the [first local run](./workflows/first-local-run). For a simulation through a separately managed API, use the [SimGrid tutorial](./workflows/first-run). To connect another target, see [Environments](./infrastructure/environments).
